name: Scheduled SDK Health Check

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

concurrency:
  group: scheduled-coin-flip-sdk-health-check
  cancel-in-progress: true

jobs:
  scheduled-verify:
    name: Verify coin-flip-sdk from npm
    runs-on: ubuntu-latest
    steps:
      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Prepare clean environment
        run: mkdir -p /tmp/coin-flip-sdk-check

      - name: Initialize project
        working-directory: /tmp/coin-flip-sdk-check
        run: npm init -y

      - name: Fetch coin-flip-sdk from registry
        working-directory: /tmp/coin-flip-sdk-check
        run: npm install coin-flip-sdk

      - name: Confirm SDK require succeeds
        working-directory: /tmp/coin-flip-sdk-check
        run: node -e "const sdk = require('coin-flip-sdk'); console.log('Scheduled health check: coin-flip-sdk OK');"

      - name: Show dependency tree
        working-directory: /tmp/coin-flip-sdk-check
        run: npm ls --all
